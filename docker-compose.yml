services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    depends_on:
      - mongodb
    # Optional: Mount the app directory for live-reloading during development
    volumes:
      - ./app:/app/app 
      - ./tests:/app/tests

  mongodb:
    image: mongo:latest
    ports:
      - "27017:27017"
    volumes:
      # Persist database data across container restarts
      - mongodb_data:/data/db
    healthcheck:
      test: ["CMD", "mongosh", "--quiet", "--eval", "db.adminCommand('ping')"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  mongodb_data: